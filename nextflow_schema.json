{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/vina/master/nextflow_schema.json",
    "title": "nf-core/vina pipeline parameters",
    "description": "High-throughput, containerized molecular docking using AutoDock Vina with modern ligand and receptor preparation",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["input", "outdir"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "schema": "assets/schema_input.json",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to comma-separated file containing information about the docking jobs.",
                    "help_text": "You will need to create a samplesheet with information about receptor-ligand pairs before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with at minimum 3 columns (sample, receptor, ligand), and a header row. Optional columns include center_x, center_y, center_z, size_x, size_y, size_z for per-sample docking box definitions. See [usage docs](https://nf-co.re/vina/usage#samplesheet-input).",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "docking_box_options": {
            "title": "Docking box options",
            "type": "object",
            "fa_icon": "fas fa-cube",
            "description": "Define the search space for molecular docking. These are global defaults that can be overridden per-sample in the samplesheet.",
            "properties": {
                "center_x": {
                    "type": "number",
                    "description": "X coordinate of the docking box center (Angstroms).",
                    "fa_icon": "fas fa-arrows-alt-h",
                    "help_text": "The X coordinate of the center of the search space. This can also be specified per-sample in the samplesheet."
                },
                "center_y": {
                    "type": "number",
                    "description": "Y coordinate of the docking box center (Angstroms).",
                    "fa_icon": "fas fa-arrows-alt-v",
                    "help_text": "The Y coordinate of the center of the search space. This can also be specified per-sample in the samplesheet."
                },
                "center_z": {
                    "type": "number",
                    "description": "Z coordinate of the docking box center (Angstroms).",
                    "fa_icon": "fas fa-arrows-alt",
                    "help_text": "The Z coordinate of the center of the search space. This can also be specified per-sample in the samplesheet."
                },
                "size_x": {
                    "type": "number",
                    "default": 20,
                    "description": "Size of the docking box in X dimension (Angstroms).",
                    "fa_icon": "fas fa-ruler-horizontal",
                    "help_text": "The size of the search space in the X dimension. Default is 20 Angstroms."
                },
                "size_y": {
                    "type": "number",
                    "default": 20,
                    "description": "Size of the docking box in Y dimension (Angstroms).",
                    "fa_icon": "fas fa-ruler-vertical",
                    "help_text": "The size of the search space in the Y dimension. Default is 20 Angstroms."
                },
                "size_z": {
                    "type": "number",
                    "default": 20,
                    "description": "Size of the docking box in Z dimension (Angstroms).",
                    "fa_icon": "fas fa-ruler",
                    "help_text": "The size of the search space in the Z dimension. Default is 20 Angstroms."
                }
            }
        },
        "docking_parameters": {
            "title": "Docking parameters",
            "type": "object",
            "fa_icon": "fas fa-atom",
            "description": "Parameters controlling the AutoDock Vina docking algorithm.",
            "properties": {
                "exhaustiveness": {
                    "type": "integer",
                    "default": 8,
                    "minimum": 1,
                    "description": "Exhaustiveness of the global search.",
                    "fa_icon": "fas fa-search",
                    "help_text": "Higher values increase the probability of finding the global minimum but also increase computation time. Recommended values: 8 for quick screens, 32 for production runs, 64+ for high-accuracy studies."
                },
                "num_modes": {
                    "type": "integer",
                    "default": 9,
                    "minimum": 1,
                    "maximum": 20,
                    "description": "Maximum number of binding modes to generate.",
                    "fa_icon": "fas fa-layer-group",
                    "help_text": "The maximum number of docking poses to output, ranked by binding affinity."
                },
                "energy_range": {
                    "type": "number",
                    "default": 3,
                    "minimum": 0,
                    "description": "Maximum energy difference from the best mode (kcal/mol).",
                    "fa_icon": "fas fa-bolt",
                    "help_text": "Poses with binding energy more than this value above the best pose will not be reported."
                }
            }
        },
        "molecule_preparation": {
            "title": "Molecule preparation options",
            "type": "object",
            "fa_icon": "fas fa-flask",
            "description": "Parameters for receptor and ligand preparation.",
            "properties": {
                "receptor_ph": {
                    "type": "number",
                    "default": 7.4,
                    "minimum": 0,
                    "maximum": 14,
                    "description": "pH for receptor protonation.",
                    "fa_icon": "fas fa-prescription-bottle",
                    "help_text": "The pH value used to determine the protonation state of ionizable residues in the receptor. Default is physiological pH (7.4)."
                },
                "ligand_ph": {
                    "type": "number",
                    "default": 7.4,
                    "minimum": 0,
                    "maximum": 14,
                    "description": "pH for ligand protonation.",
                    "fa_icon": "fas fa-pills",
                    "help_text": "The pH value used to determine the protonation state of ionizable groups in the ligand. Default is physiological pH (7.4)."
                },
                "remove_water": {
                    "type": "boolean",
                    "default": true,
                    "description": "Remove water molecules from receptor PDB.",
                    "fa_icon": "fas fa-tint-slash",
                    "help_text": "Remove water molecules (HOH, WAT, H2O, TIP, SPC, SOL residues) from the receptor structure before preparation. This is recommended for docking."
                },
                "remove_heteroatoms": {
                    "type": "boolean",
                    "default": false,
                    "description": "Remove all heteroatoms from receptor PDB.",
                    "fa_icon": "fas fa-eraser",
                    "help_text": "Remove all HETATM records including waters, ligands, ions, and cofactors from the receptor. Use this when you want a clean protein-only structure."
                }
            }
        },
        "virtual_screening": {
            "title": "Virtual screening options",
            "type": "object",
            "fa_icon": "fas fa-th",
            "description": "Options for high-throughput virtual screening with multiple receptors and/or ligands.",
            "properties": {
                "screening_mode": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable virtual screening mode.",
                    "fa_icon": "fas fa-sitemap",
                    "help_text": "When enabled, the pipeline generates all combinations of receptors × ligands from the samplesheet. Use this for screening many ligands against one or more targets."
                },
                "ligand_library": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to multi-molecule SDF file for virtual screening.",
                    "fa_icon": "fas fa-database",
                    "help_text": "A single SDF file containing multiple ligand molecules for virtual screening. Each molecule will be extracted and docked against all receptors."
                },
                "receptor_list": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to file listing receptor PDB files.",
                    "fa_icon": "fas fa-list",
                    "help_text": "A text file with one receptor PDB path per line. All ligands will be docked against all receptors in this list."
                }
            }
        },
        "binding_site_detection": {
            "title": "Binding site detection options",
            "type": "object",
            "fa_icon": "fas fa-crosshairs",
            "description": "Options for automatic binding site detection from co-crystallized ligands.",
            "properties": {
                "auto_binding_site": {
                    "type": "boolean",
                    "default": false,
                    "description": "Auto-detect binding site from co-crystallized ligand.",
                    "fa_icon": "fas fa-magic",
                    "help_text": "When enabled, the pipeline automatically detects the docking box center and size from co-crystallized ligands in the receptor PDB file. This is useful when the receptor comes from an X-ray co-crystal structure."
                },
                "ligand_id": {
                    "type": "string",
                    "description": "Specific ligand residue name to use for binding site detection.",
                    "fa_icon": "fas fa-tag",
                    "help_text": "If the PDB contains multiple ligands, specify the 3-letter residue name (e.g., 'MK1', 'ATP') to use for binding site detection. If not specified, the largest ligand will be used."
                },
                "box_padding": {
                    "type": "number",
                    "default": 5,
                    "minimum": 0,
                    "description": "Padding around the ligand for box size (Angstroms).",
                    "fa_icon": "fas fa-expand",
                    "help_text": "The padding in Angstroms to add around the co-crystallized ligand when calculating the docking box size. Default is 5 Angstroms on each side."
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "hook_url": {
                    "type": "string",
                    "description": "Incoming hook URL for messaging service",
                    "fa_icon": "fas fa-people-group",
                    "help_text": "Incoming hook URL for messaging service. Currently, MS Teams and Slack are supported.",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "pipelines_testdata_base_path": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "Base URL or local path to location of pipeline test dataset files",
                    "default": "https://raw.githubusercontent.com/nf-core/test-datasets/",
                    "hidden": true
                },
                "trace_report_suffix": {
                    "type": "string",
                    "fa_icon": "far calendar",
                    "description": "Suffix to add to the trace report filename. Default is the date and time in the format yyyy-MM-dd_HH-mm-ss.",
                    "hidden": true
                },
                "help": {
                    "type": ["boolean", "string"],
                    "description": "Display the help message."
                },
                "help_full": {
                    "type": "boolean",
                    "description": "Display the full detailed help message."
                },
                "show_hidden": {
                    "type": "boolean",
                    "description": "Display hidden parameters in the help message (only works when --help or --help_full are provided)."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/docking_box_options"
        },
        {
            "$ref": "#/$defs/docking_parameters"
        },
        {
            "$ref": "#/$defs/molecule_preparation"
        },
        {
            "$ref": "#/$defs/virtual_screening"
        },
        {
            "$ref": "#/$defs/binding_site_detection"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
